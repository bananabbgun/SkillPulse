__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.streamlit/
# Keep marts (CSV) + figures (PNG) under output/ so the Streamlit Cloud deploy
# can render without re-running the pipeline server-side. Exclude heavier
# binary intermediates (Parquet, DuckDB).
output/marts/*.parquet
output/marts/*.duckdb
data/raw/events.jsonl
data/raw/postings_collected.json
data/raw/_*
data/raw/*.log
data/lake/
data/.playwright_profile/

